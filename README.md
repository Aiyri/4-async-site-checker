# 4-async-site-checker
An async site availability checker built with asyncio + aiohttp, using a semaphore to control concurrency. Distinguishes timeouts from network errors. Tests run against a local test HTTP server.
